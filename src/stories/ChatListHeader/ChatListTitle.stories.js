import React from 'react';
import { ChatListTitle } from '../../components/ChatListHeader.js';

export default {
	title: 'Chat list header title',
	component: ChatListTitle,
};

export const Simple = () => <ChatListTitle />;
