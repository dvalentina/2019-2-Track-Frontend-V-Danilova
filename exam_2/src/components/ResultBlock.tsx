import React from 'react'
import * as T from '../types/ResultBlock.types'

export default function ResultBlock(props: T.IResultBlockProps) {
    let result
    return (
        <div>
            {result}
        </div>
    )
}